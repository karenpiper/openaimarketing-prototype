# Optional: the rest of the workshop works without these settings.
OPENAI_API_KEY=
# Choose a Responses API model that supports Structured Outputs in your account.
OPENAI_MODEL=
# A private facilitator code; required to prevent anonymous use of the paid endpoint.
WORKSHOP_ACCESS_CODE=
